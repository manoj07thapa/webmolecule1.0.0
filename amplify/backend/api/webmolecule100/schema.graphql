type Course
  @model(subscriptions: null)
  @auth(
    rules: [
      # allow all guest users (not authenticated) to view books
      { allow: public, operations: [read], provider: iam }
      # allow admins to create, update and delete books
      { allow: groups, groups: ["Admin"] }
      # allow all authentic,ated users to view books
      { allow: owner }
    ]
  ) {
  id: ID!
  title: String!
  subtitle: String
  category: String!
  files: [String]
  price: Float!
  language: String!
  tutor: String!
  duration: String!
  framework: String!
  reqKnowledge: String!
  level: String!
  time: String!
  courseContent: [Syllabus]
}

type Syllabus {
  topic: String
  description: String
  duration: String
}
